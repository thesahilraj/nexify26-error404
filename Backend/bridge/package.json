{
  "name": "farm-iot-bridge",
  "version": "1.0.0",
  "description": "Serial bridge: reads ESP32 sensor data from USB and serves it via HTTP",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "serialport": "^12.0.0",
    "express": "^4.21.0",
    "cors": "^2.8.5"
  }
}
